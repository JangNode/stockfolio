# 반드시 지킬 것

1. **point-in-time 데이터 누수 금지.** 백테스트/스크리닝은 그 시점에 실제로
   알 수 있었던 데이터만 쓴다. 결산·공시 이후에나 확정되는 값을 그 이전
   시점의 판단에 섞지 않는다.
2. **기준값은 항상 상수로 분리한다.** 매직넘버를 코드에 직접 쓰지 않는다.
   `lib/*Config.ts`에 출처 주석과 함께 둔다 — 나중에 값을 바꾸거나 감사할 때
   그 파일 하나만 보면 되게 한다.
3. **DB 용량 예산을 항상 고려한다.** 무료 공유 용량이 빠듯하다. 불필요한
   원문·본문 저장을 피하고, 보관 기간 정책을 반드시 두며, 가능하면 변경점만
   저장한다.
4. **불필요한 외부 API 호출을 늘리지 않는다.** 무료 한도 안에서 필요한
   만큼만 호출한다. 폴링 주기는 "이 데이터가 실제로 얼마나 자주 바뀌는가"로
   정하지, 습관적으로 짧게 잡지 않는다.
5. **이미 적용된 Supabase 마이그레이션 파일은 수정하지 않는다.** 되돌리거나
   바꿔야 하면 항상 새 마이그레이션을 추가한다.
6. **사용자의 명시적 확인 없이 프로덕션 데이터를 삭제·수정하지 않는다.**
   의심스러운 데이터를 발견해도 스스로 판단해 지우지 않고, 조사 결과를
   보고한 뒤 확인을 받는다.
7. **LLM(Anthropic API) 기반 해석·분류·요약 기능은 요청받지 않으면 임의로
   추가하지 않는다.** 특히 판단이 필요한 기능(성향 분류, 자동 매매 신호 등)은
   신뢰도를 먼저 검증하기 전까지 보류한다.
8. **필요한 만큼만 구현한다.** 과잉 설계, 미래를 위한 추상화, 쓰이지 않는
   옵션·플래그를 만들지 않는다. 세 줄이 비슷하면 세 줄 그대로 두는 게 성급한
   공통화보다 낫다.
9. **계획을 먼저 보여달라는 요청엔 구현 전에 계획부터 제시하고 확인을
   받는다.** 설계가 원래 요청보다 커지는 부분(예: 자동화 범위 확장, 새로운
   인프라 패턴 도입)은 반드시 별도로 짚어서 확인받는다.
