// 스크리닝 추적 종목의 시세 조회가 이 횟수만큼 연속 실패하면(배치가 하루 1회
// 실행되므로 사실상 "N일 연속") status를 'price_unavailable'로 전환한다.
// withRetry가 이미 요청 하나당 속도제한 재시도를 소진한 뒤에야 실패로 기록되므로
// 하루치 실패도 어느 정도 신뢰도가 있지만, KIS 쪽 일시 장애 같은 우연을 배제하기
// 위해 최소 1회는 더 지켜본다. 이 상태 전환은 강제청산을 "보류"시키고 배지를
// 띄우는 것뿐이라 오탐 비용이 낮아 짧게 잡아도 안전하다(2026-09-20 판단).
export const PRICE_FETCH_FAILURE_THRESHOLD = 3;
