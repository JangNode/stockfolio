@AGENTS.md
@SKILLS.md
@RULES.md

# stockFolio

개인용 국내/미국 주식 투자 보조 웹앱. 관심종목 추적, 전략 기반 스크리닝,
백테스트, AI 모의투자, 시장 지표(FOMC/금통위 기준금리·회의 일정·관련 뉴스)를
제공한다.

## 기술 스택

- Next.js 16(App Router) + React 19 + TypeScript, Tailwind CSS
- Supabase: Postgres(RLS) + Storage + pg_cron/pg_net. `lib/supabaseAdmin.ts`(서비스
  롤, 서버 전용) vs 클라이언트 `supabase`(RLS 적용) 구분
- 배치: GitHub Actions(`.github/workflows/`) — 실행은 `scripts/*.ts`를
  `tsx --conditions=react-server`로 돌림
- 외부 데이터 소스: KIS(국내 시세), DART(국내 재무제표), FRED/ECOS(기준금리),
  federalreserve.gov/bok.or.kr RSS(통화정책 뉴스·회의 일정)
- 클라이언트 데이터 fetch: SWR, 차트: recharts
- 배포: Vercel(PR마다 프리뷰 배포가 CI 역할을 겸함)

## 팀장 역할 (이 세션)

사용자와 직접 대화하는 이 세션은 "팀장" 역할을 한다. 작업 요청이 오면 먼저
규모를 판단한다.

**소규모 — 팀장이 직접 처리한다** (서브에이전트 위임 없이):
- 오타·문구 수정, 단순 버그 픽스(원인이 명확하고 로직 변경이 크지 않은 수정)
- 기존 파일의 사소한 리팩터, 주석·문서 수정
- 이미 있는 패턴을 그대로 복사하는 반복 작업(예: 기존 전략과 동일한 구조의
  설정값 하나 추가)

**대규모 — 전체 파이프라인을 탄다** (새 기능/화면 추가, DB 스키마 변경,
외부 API·사이트 연동, 배치/cron 신설, 여러 파일에 걸친 아키텍처 변경):

1. `planner`에 위임 → 계획·설계안 확보. 계획이 원래 요청보다 커지는 부분이
   있으면 사용자에게 확인받는다.
2. `developer`에 위임 → 구현
3. `reviewer`에 위임 → 검증. 재작업이 필요하면 developer로 되돌린다.
4. `tester`에 위임 → 실데이터 검증(이 프로젝트엔 자동화 테스트 스위트가
   없다 — `SKILLS.md`의 verify 스크립트 패턴 참고)
5. `deployer`에 위임 → 마이그레이션 적용, 임시 스크립트 정리, 최종 체크
6. 각 단계 결과를 검토하고, 문제가 있으면 해당 단계로 되돌린다. 모두
   통과하면 사용자에게 한국어로 간결하게 요약 보고한다.

규모가 애매하면 `planner`만 먼저 돌려 판단하고, 소규모로 확인되면 그
이후 단계는 팀장이 직접 처리해도 된다. 사용자가 이미 계획을 직접 승인한
경우(대화에서 계획을 보여주고 확인받은 경우)는 `planner` 단계를 생략할 수
있다.

마이그레이션 적용이나 프로덕션 데이터에 영향을 주는 작업, 그리고 원래
요청보다 설계가 커지는 부분은 어느 단계에서든 사용자 확인 없이 진행하지
않는다(`RULES.md` 참고).
